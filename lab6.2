using System; 
using System.Collections;
interface IFigure {

    double Area{ get;}
    string PrintValues();
}

public class ArrayFigures : IEnumerable {

    string[] figures;

    public ArrayFigures( string[] a) {

        figures = a;
    }

    IEnumerator IEnumerable.GetEnumerator() {
         
         return figures.GetEnumerator();
    }
}

public class Square: IFigure
{
    private int side;
    public Square(int side)
    {
        this.side = side;
    }
    public int Side
    {
       get {return side;}
    }
    public  double Area
    {
        get {return Side*Side;}
    }
    public int Perimetr
    {
        get {return 4*Side;}
    }
    public string PrintValues()
    {
        string s = "Сторона квадрата: " + Side + "\n";
        string p = "Периметр квадрата: " + Perimetr + "\n";
        string a = "Площадь квадрата: " + Area + "\n";
        return "\n" + s + p + a;
    }
}

public class Circle: IFigure
{
    private int radius;
    private string color;
    public Circle(int radius, string color)
    {
        this.radius = radius;
        this.color = color;
    }
    public int Radius
    {
       get {return radius;}
    }
    public string Color
    {
       get {return color;}
    }
    public double Area
    {
        get {return 3.14*radius*radius;}
    }
    public string PrintValues()
    {
        string r = "Радиус круга: " + Radius + "\n";
        string c = "Цвет круга: " + Color + "\n";
        string a = "Площадь круга: " + Area + "\n";
        return "\n" + r + c + a;
    }
}

public class Program
{
	public static void Main()
	{
	    Square f1 = new Square(15);
	    Square f2 = new Square(4);
        Square f3 = new Square(3);
	    Square f4 = new Square(9);
	    Circle f5 = new Circle(10, "red");
	    Circle f6 = new Circle(2, "white");
        Circle f7 = new Circle(9, "blue");
	    Circle f8 = new Circle(5, "green");
	    
        string[] figures = new string[] {f1.PrintValues(), f2.PrintValues(), f3.PrintValues(), f4.PrintValues(), f5.PrintValues(), f6.PrintValues(), f7.PrintValues(), f8.PrintValues()};
	    
	    ArrayFigures arr = new ArrayFigures(figures);
	    foreach (string a in arr)
	    {
	        Console.WriteLine(a);
            System.Console.WriteLine("----------------------------------");
	    }
	}
}
